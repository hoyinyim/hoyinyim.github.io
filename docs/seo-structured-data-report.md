# SEO 與結構化資料報告

驗收日期：2026-07-21。

- Lighthouse SEO：100。
- 11 頁均有 `zh-Hant`、唯一 Title、Description、Canonical、Open Graph、Twitter Card、Theme Color 與 Build Commit Metadata。
- 首頁提供 `ProfilePage`／`Person`；期刊與研討會提供 `ItemList` JSON-LD，建置期生成且可解析。
- `sitemap.xml`、`robots.txt`、`rss.xml`、404 與靜態 Search Index 完整。
- Open Graph 使用本機無文字抽象圖 `images/og-default.png`；尺寸 1730 × 909，不依賴外部伺服器。
- 全站主要內容存在於初始 HTML；JavaScript 關閉時仍可讀取論文、學歷、作品與證照資料。
